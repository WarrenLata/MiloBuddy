# Infra (placeholders)

This folder is intended for Terraform or GCP deployment configs.

Suggested contents:
- `main.tf` — terraform resources for Cloud Run, service account, IAM bindings.
- `variables.tf` — configuration variables.

Replace with production-safe infra and review IAM scopes before applying.
